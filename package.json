{
  "dependencies": {},
  "private": "true",
  "scripts": {
    "start": "wintersmith preview",
    "build": "npm run clean && wintersmith build",
    "clean": "rm -rf build/assets && rm -rf build/*.html",
    "deploy-gh-pages": "git subtree push --prefix build origin gh-pages"
  },
  "devDependencies": {
    "wintersmith": "^2.2.1",
    "wintersmith-libsass": "^0.3.2",
    "wintersmith-makerelative": "^1.0.0"
  }
}
